# 代币部署学习笔记 (Sepolia 测试网)

**代币名称:** LianaToken  **代币符号:** LNT 
##  一、ERC20 与 ERC721 区别

| **特征**   | **ERC20                      | **ERC721 (NFT)**            |
| -------- | ---------------------------- | --------------------------- |
| **同质化**  | **可互换 。任意一个 LNT 价值相同，可以互相替代。 | **不可互换** 每个代币都有唯一的 ID，独一无二。 |
| **记账方式** | 记录**数量/余额**                  | 记录**唯一 ID 列表**              |
| **用途**   | 货币、通用积分、平台内支付、投票权。           | 艺术品、收藏品、身份认证、产权证明。          |

##  二、部署操作步骤详解

### 1. 钱包与环境准备

| **步骤**        | **操作内容**                    | **关键点**                   |
| ------------- | --------------------------- | ------------------------- |
| **创建 EVM 地址** | 使用 MetaMask 创建钱包账户。         | **助记词和私钥** 必须安全离线保存，不可泄露！ |
| **网络切换**      | 点击显示测试网络，切换到 **Sepolia 网络** | 这是部署的必要环境。                |
| **获取测试币**     | 第三方平台购买，比较方便快捷，我认为          | 确保有 Gas 费才能进行部署。          |

### 2. ERC20 合约编写 (LianaToken.sol)

使用 **Remix IDE** 编写合约。

**LianaToken.sol 代码：**

```
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.20;
import "@openzeppelin/contracts/token/ERC20/ERC20.sol";
import "@openzeppelin/contracts/access/Ownable.sol";
// 合约名称：LianaToken
// 继承 ERC20 和 Ownable 的所有功能
contract LianaToken is ERC20, Ownable {
    constructor() 
        //调用 ERC20 构造函数，设定代币的名称和符号
        //代币全名: "LianaToken"
        //代币符号: "LNT" 
        ERC20("LianaToken", "LNT") 
        
        //调用 Ownable 构造函数，将合约的所有者设为部署者
        Ownable(msg.sender)
    {
        //设定初始发行量：10,000,000 个代币
        uint256 initialSupply = 10000000 * 10**decimals(); 

        _mint(msg.sender, initialSupply);
    }

}
```

### 3. 连接与部署

1. **连接 Remix:** 在 Remix 的 **Deploy & Run Transactions** 选项卡中，选择 **"Injected Provider - MetaMask"**，授权连接。
2. **编译合约:** 确保 `lianaToken.sol` 编译成功。
3. **执行部署:** 选择 `LianaToken` 合约，点击 **Deploy**，并在 MetaMask 中确认 Gas 费交易。
## 📄 三、部署成果与提交信息

| **代币名称 **   | **LianaToken**                                                       |
| ----------- | -------------------------------------------------------------------- |
| **代币符号 **   | **LNT**                                                              |
| **总发行量 **   | **10,000,000 LNT**                                                   |
| **合约部署地址 ** | `0xc01397849a9999a83B4843617504C6757fF66774`                         |
| **部署交易哈希 ** | `0xfa37bc42033c2780b46d279eaec8d14f7c4347d10c020727dd549171a77b3541` |

## 四、GitHub 提交步骤 

1. **克隆 Fork 仓库：**
```
git clone [https url]
```
2. **进入baby-dev**
```
cd baby-dev
```
3. **手动创建笔记与代码文件**
4. **推送到 GitHub：**
  ```
git add .
git commit -m"complete week1 task"
git push origin main
  ```
5. **创建 PR：** 访问 GitHub 网页，从自己的 Fork 仓库向主仓库提交。